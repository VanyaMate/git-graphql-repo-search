import React from 'react';
import css from './PaginationEmpty.module.scss';

const PaginationEmpty = () => {
    return (
        <span className={css.empty}>
            ...
        </span>
    );
};

export default PaginationEmpty;